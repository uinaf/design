interface:
  display_name: "uinaf design"
  short_description: "Route uinaf work to the live design contract"
  default_prompt: "Use $uinaf-design to fetch the live design contract before changing this explicitly scoped uinaf interface or repository copy."

policy:
  allow_implicit_invocation: false
